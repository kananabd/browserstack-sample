/**
 * Functions to work with encryption using AWS KMS services
 *
 * @export
 * @class Encrypt
 */
export declare class Encrypt {
    private ENCRYPT_KEY_ID;
    private static instance;
    private readonly _kms;
    /**
     * Return instance of the Encrypt class
     */
    static getInstance(): Encrypt;
    /**
     * Encrypt data
     * @param data to encrypt
     */
    encrypt(data: string): Promise<string>;
    /**
     * Decrypt data
     * @param data in the base64 format to decrypt
     */
    decrypt(encdata: string): Promise<string>;
}
