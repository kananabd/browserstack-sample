import { DynamoDbBase } from "./dynamodbbase";
import { FeatureFlag } from "./models/featureflagmodel";
export interface FeatureFlagInfo {
    name: string;
    value: boolean;
    inherited?: boolean;
}
export declare class FeatureFlagRepository extends DynamoDbBase {
    private readonly systemInstanceId;
    /**
     * Returns feature flags by given identifier
     * @param {string[]} instanceIds - an unique feature flags identifiers
     * @returns {Promise<FeatureFlag[]>} - promised object to get feature flags
     */
    getFeatureFlags(instanceIds: string[]): Promise<FeatureFlag[] | null>;
    getMergedFlags(instanceId: string): Promise<FeatureFlagInfo[] | undefined>;
    /**
     * Updates the feature flags
     * @param featureFlag
     * @returns {Promise<void>}
     */
    updateFeatureFlag(featureFlag: FeatureFlag): Promise<void>;
    /**
     * Deletes the feature flags
     * @param instanceId
     * @returns {Promise<void>}
     */
    deleteFeatureFlag(instanceId: string): Promise<void>;
    private invalidateInstanceCache;
}
