export declare class BatchBehavior<T> {
    private batchSize;
    private batchItems;
    private fnFlush;
    constructor(batchSize: number, flush: (batchItems: T[]) => Promise<void>);
    push(input: T): Promise<void>;
    flush(): Promise<void>;
}
