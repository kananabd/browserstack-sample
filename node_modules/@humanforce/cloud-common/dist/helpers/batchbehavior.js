"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.BatchBehavior = void 0;
class BatchBehavior {
    constructor(batchSize, flush) {
        this.batchItems = [];
        this.batchSize = batchSize;
        this.fnFlush = flush;
    }
    async push(input) {
        this.batchItems.push(input);
        if (this.batchItems.length === this.batchSize) {
            await this.flush();
            this.batchItems = [];
        }
    }
    async flush() {
        if (this.batchItems.length) {
            await this.fnFlush(this.batchItems);
        }
    }
}
exports.BatchBehavior = BatchBehavior;
