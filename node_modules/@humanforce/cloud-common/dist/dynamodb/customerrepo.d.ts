import { DynamoDbBase } from "./dynamodbbase";
import { Customer } from "./models/customermodel";
export declare class CustomerRepository extends DynamoDbBase {
    /**
     * Returns customer by given identifier
     * @param {string} customerId - an unique customer identifier
     * @returns {Promise<Customer>} - promised object to get customer
     */
    getCustomer(customerId: string): Promise<Customer | null>;
    /**
     * Get customers
     * @param customerIds - identifiers of customers, if empty all records will be returned
     */
    getAllCustomers(customerIds?: string[]): Promise<Customer[]>;
    /**
     * Returns customer by sfo guid key
     * @param userId - user identifier
     */
    getCustomerBySfoGuidKey(sfoGuidKey: string): Promise<Customer | null>;
    /**
     * Returns customer by client code
     * @param clientCode - client code
     */
    getCustomerByClientCode(clientCode: string): Promise<Customer | null>;
    /**
     * Updates the customer
     * @param customer
     * @returns {Promise<void>}
     */
    updateCustomer(customer: Customer): Promise<void>;
    /**
     * Returns customer by name
     * @param name - customer name
     */
    getCustomerByName(name: string): Promise<Customer | null>;
}
