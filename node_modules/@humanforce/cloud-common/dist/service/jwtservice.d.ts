/**
 * Service to work with JWT
 */
export declare class JwtService {
    private readonly kms;
    private readonly rkmGuid;
    private readonly signingAlgorithm;
    private readonly signingAlgorithmHeader;
    generateJwt(signingKeyId: string, instanceId: string, hfUserId: string, audience: string, issuer: string, sub?: string | undefined, expirationTime?: number): Promise<string>;
    validateJwt(message: Uint8Array, signature: Uint8Array, signingKeyId: string): Promise<boolean>;
}
