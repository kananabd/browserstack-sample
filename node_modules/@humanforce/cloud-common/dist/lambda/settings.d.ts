/**
 * Class which wraps all settings used by lambda (mostly a wrapper against env variables)
 *
 * @export
 * @class Settings
 */
export declare class LambdaSettings {
    /**
     * default timeout period for all outgoing http requests
     */
    static defaultTimeout: number;
}
