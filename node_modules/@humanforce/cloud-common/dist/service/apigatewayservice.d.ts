/**
 * Service to work with AWS APi Gateway service service
 */
export declare class ApiGatewayService {
    invalidateCacheByKey(endpoint: string, key: string): Promise<void>;
    /**
     * Returns hosted zone
     */
    private getHostedZone;
}
