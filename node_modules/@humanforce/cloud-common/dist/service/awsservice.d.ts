import { AwsCredentialIdentity } from "@smithy/types";
export declare class AWSService {
    private sts;
    private credentials?;
    assumeRole(roleArn: string): Promise<void>;
    protected getAWSCredentials(): AwsCredentialIdentity | undefined;
}
