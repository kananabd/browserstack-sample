export declare class ParameterStoreService {
    private readonly _client;
    getParameter(name: string, withDecryption?: boolean): Promise<string>;
    getParameters(path: string, withDecryption?: boolean, recursive?: boolean): Promise<Map<string, string>>;
}
