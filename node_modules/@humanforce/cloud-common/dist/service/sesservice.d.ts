export type TemplateData = {
    [key: string]: string;
};
export declare class SESService {
    private readonly _ses;
    /**
     * This method send an email.
     * @param addressesTo - email recipient(s)
     * @param subject - email subject
     * @param message - email body
     * @param isHtml - if it is true then message will be sent as html message, otherwise as plain text
     */
    sendEmail(addressesTo: string | string[], subject: string, message: string, isHtml?: boolean, addressesCc?: string | string[], emailSender?: string): Promise<void>;
    /**
   * This method send an templated email.
   * @param addressesTo - email recipient(s)
   * @param templateName - name of template uploaded to SES
   * @param templateData - data for template as key-value pairs of string
   */
    sendTemplatedEmail(addressesTo: string | string[], templateName: string, templateData: TemplateData, addressesCc?: string | string[], emailSender?: string): Promise<void>;
    private getSenderSource;
}
