import { DynamoDbBase } from "./dynamodbbase";
import { IntegrationSetting } from "./models/integrationsettingsmodel";
export declare class IntegrationSettingsRepository extends DynamoDbBase {
    /**
     * Returns integration settings by given identifier
     * @param {string} instanceId - an unique integration settings identifier
     * @param {boolean} deleted - flag indicating that the entry is active
     * @returns {Promise<IntegrationSetting>} - promised object to get integration settings
     */
    getIntegrationSetting(instanceId: string, deleted?: boolean): Promise<IntegrationSetting | null>;
    getAllIntegrationSetting(): Promise<IntegrationSetting[]>;
    /**
     * Updates the integration settings
     * @param setting
     * @returns {Promise<void>}
     */
    updateIntegrationSetting(setting: IntegrationSetting): Promise<void>;
    /**
     * Deletes the integration settings
     * @param instanceId
     * @returns {Promise<void>}
     */
    deleteIntegrationSetting(instanceId: string): Promise<void>;
}
